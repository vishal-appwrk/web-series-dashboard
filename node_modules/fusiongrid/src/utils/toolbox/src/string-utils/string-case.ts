/**
 * Convert the first letter of a string to upper case
 * @param str String to captilize
 */
export function captalizeFirstLetter(str?: string) {
  return typeof str === 'undefined' ? str : str.charAt(0).toLocaleUpperCase() + str.slice(1);
}