export default function formatString(...format: Array<string>): string {
  const args = format.slice(1);
  let mainStr = format[0];

  return mainStr.replace(/{(\d+)}/g, (match: string, index: number): string => {
    return typeof args[index] !== 'undefined' ? args[index] : match;
  });
}