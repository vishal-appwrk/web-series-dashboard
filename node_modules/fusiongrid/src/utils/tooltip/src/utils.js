const isDefined = (value)=> (value !== undefined) && ( !Array.isArray(value) || (Array.isArray(value) && value.length)) && (value !== null);

const isTruthy = (value)=> {
    if (value){
        return true;
    }
    return false;
}

export {
    isDefined,
    isTruthy
}