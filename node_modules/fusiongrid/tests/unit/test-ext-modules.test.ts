import {ScaleLinear} from '../../src/utils/scales/src';
import {Logger} from '../../src/utils/toolbox/src';
let Tooltip = require('../../src/utils/tooltip/src');

describe('modules import check', () => {
  it('scales working', (done) => {
    let scale =  new ScaleLinear();
    done();
  });
  it('toolbox working', (done) => {
    let logger =  new Logger(()=>{});
    done();
  });
  it('tooltip working', (done) => {
    let tooltip =  new Tooltip();
    done();
  });
});