import DataStore from './datastore';
import { between, equals, less, lessEquals, greater, greaterEquals, IN, contains, startsWith, endsWith,
  filter, or, not, select, groupBy, pipe, sort, pivot } from './operators';
import AggregatorStore from './aggregators';
import { before, after, parseDate, formatDate, duration } from './time-utils/src/index';
import { DatetimeUnits, Weekdays  } from './toolbox/src/index';

let FusionDataStore = DataStore;

FusionDataStore.Operators = {
  between,
  equals,
  less,
  lessEquals,
  greater,
  greaterEquals,
  IN,
  contains,
  startsWith,
  endsWith,
  filter,
  or,
  not,
  select,
  groupBy,
  pipe,
  sort,
  pivot
};

FusionDataStore.Utils = {
  duration,
  before,
  after,
  parseDate,
  formatDate,
  DatetimeUnits,
  Weekdays
};

FusionDataStore.Aggregators = {
  AggregatorStore
};

export default FusionDataStore;