export default function formatString(...format) {
  const args = format.slice(1);
  let mainStr = format[0];

  return mainStr.replace(/{(\d+)}/g, (match, index) => {
    return typeof args[index] !== 'undefined' ? args[index] : match;
  });
}