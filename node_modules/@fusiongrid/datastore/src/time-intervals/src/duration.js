import { DatetimeUnits } from '../../toolbox/src/index';

function duration (Unit, _number) {
  let number = _number;
  // If Unit is not one of predefined, throw an error.
  if (!Unit || !DatetimeUnits[Unit.name]) throw new Error(`Invalid Unit provided: ${Unit}`);

  number = Math.abs(number) || 1;

  return {
    Unit: Unit.name,
    number: number,
    ms: Unit.ms * number
  };
}

export { duration };
