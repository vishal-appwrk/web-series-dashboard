import MillisecondInterval from './time-interval-ms.js';

const millisecond = new MillisecondInterval(
  'millisecond',
  () => { /* no-op */ },
  (d, s) => d.setTime(Number(d) + Number(s)),
  (s, e) => e - s
);

export default millisecond;
