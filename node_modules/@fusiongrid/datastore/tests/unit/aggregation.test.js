import aggregatorStore from '../../src/aggregators';

describe('test aggregation class', () => {
  it('register and resolve a aggregator in the store', () => {
    var fn = () => { /* this is the fn function */ };

    aggregatorStore.register('testMethod', fn);

    expect(aggregatorStore.resolve('testMethod')).toEqual(fn);
  });
});
